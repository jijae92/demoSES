# Development and Testing Dependencies

# Core dependencies (from requirements.txt)
-r requirements.txt

# Testing
pytest>=8.0,<9
pytest-cov>=4.1,<5
pytest-mock>=3.12,<4
pytest-asyncio>=0.23,<1

# HTTP mocking
responses>=0.24,<1
pytest-httpx>=0.30,<1

# Coverage
coverage[toml]>=7.4,<8

# Code quality
ruff>=0.1,<1
black>=23.0,<24
mypy>=1.8,<2

# AWS mocking (for SES tests)
moto[ses]>=5.0,<6
